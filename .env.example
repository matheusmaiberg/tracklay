# ============================================================
# TRACKLAY - LOCAL DEVELOPMENT ENVIRONMENT
# ============================================================
# Copy this file to .env and fill in your secrets
# .env is automatically loaded by 'wrangler dev'
# This file is gitignored for security - NEVER commit it!
#
# For production, use: wrangler secret put <KEY>
# ============================================================

# ============================================================
# REQUIRED SECRETS (Generate these before running)
# ============================================================

# Obfuscation secret for hash generation (UUID format recommended)
# Generate: node -e "console.log(require('crypto').randomUUID())"
OBFUSCATION_SECRET=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# API Token for /endpoints authentication (any secure random string)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENDPOINTS_API_TOKEN=your-secure-random-token-here

# ============================================================
# CLOUDFLARE CLI AUTHENTICATION (Optional)
# ============================================================
# Only needed if you want to set account globally
# Otherwise, set account_id directly in wrangler.toml
#
# CLOUDFLARE_ACCOUNT_ID=your-account-id
# CLOUDFLARE_API_TOKEN=your-api-token

# ============================================================
# OPTIONAL - Development overrides
# ============================================================
# Uncomment to override wrangler.toml values for local testing:

# DEBUG_HEADERS_ENABLED=true
# LOG_LEVEL=debug
