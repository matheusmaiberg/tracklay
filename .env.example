# ============================================================
# TRACKLAY - WRANGLER CLI CONFIGURATION
# ============================================================
# This file is used ONLY for wrangler CLI authentication
# NOT for Worker runtime configuration
#
# Copy this file to .env and fill in your values
# This file is gitignored for security
# ============================================================

# Cloudflare Account ID (get from: wrangler whoami)
# Required for wrangler commands to know which account to use
CLOUDFLARE_ACCOUNT_ID=your-account-id-here

# Cloudflare API Token (create at: https://dash.cloudflare.com/profile/api-tokens)
# Required permissions: Cloudflare Workers (Edit), Account (Read)
CLOUDFLARE_API_TOKEN=your-api-token-here

# ============================================================
# NOTAS IMPORTANTES
# ============================================================
# 1. Este arquivo é APENAS para autenticação do CLI Wrangler
#
# 2. Para configurar o Worker, use:
#    - wrangler.toml (variáveis não-secretas)
#    - wrangler secret put (secrets de produção)
#    - .dev.vars (secrets para desenvolvimento local)
#
# 3. NUNCA coloque secrets neste arquivo - use wrangler secret put
#
# 4. Veja docs/setup/SETUP.md para guia completo de configuração
# ============================================================
